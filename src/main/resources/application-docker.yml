spring:
    datasource:
        url: jdbc:mysql://db:3306/market_dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
        username: study01
        password: 1234
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true


# 호스트명이 db 입니다. (compose 서비스명)